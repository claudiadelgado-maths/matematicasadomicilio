# José Pérez

Alumno nuevo del salón de Erik, independiente del casillero preexistente `jose`.
El índice utiliza el listado automático de `asesores/recursos/js/alumno.js`.

Sesión publicada: **Introducción**, recorrido diagnóstico acompañado desde aritmética hasta cálculo de varias variables. El propósito es orientar las próximas clases, sin examen ni nota.

Solo la sesión conserva autoevaluaciones locales, documentadas en su README. No se envían respuestas a un servidor. Mantener `usuario.json` y catálogo sincronizados; ejecutar `npm run catalogo` y `npm run validar`.
